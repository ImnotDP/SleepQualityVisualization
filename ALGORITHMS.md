# 睡眠质量分析系统 — 算法文档

> 本文档详细披露系统中使用的全部机器学习算法，包括算法原理、适用场景、参数说明及在本系统中的应用方式。

---

## 目录

1. [回归算法（预测睡眠质量评分）](#回归算法)
   - 1.1 SVR (支持向量回归)
   - 1.2 Linear Regression (线性回归)
   - 1.3 Random Forest (随机森林回归)
   - 1.4 Gradient Boosting (梯度提升回归)
   - 1.5 XGBoost (极端梯度提升)
   - 1.6 Decision Tree (决策树回归)
   - 1.7 Ridge Regression (岭回归)
   - 1.8 Lasso Regression (套索回归)
   - 1.9 ElasticNet (弹性网络)
   - 1.10 KNN Regression (K近邻回归)
   - 1.11 Bayesian Ridge (贝叶斯岭回归)
   - 1.12 AdaBoost Regression (自适应增强)
2. [分类算法（预测睡眠阶段）](#分类算法)
3. [数据预处理方法](#数据预处理)
4. [模型评估指标](#评估指标)
5. [模型对比与选择](#模型对比)

---

## <a id="回归算法"></a>1. 回归算法（预测睡眠质量评分 1-10 分）

### 1.1 SVR (支持向量回归)

**原理概述：**
支持向量回归（Support Vector Regression）是 SVM 在回归问题上的扩展。SVR 的目标是找到一个函数 $f(x)$，使得对于所有训练样本，预测值与真实值的偏差不超过 $\epsilon$，同时使函数尽可能平坦（最小化 $\|w\|^2$）。通过引入核函数（如 RBF 核），SVR 能够捕捉数据中的非线性关系。

**数学表达：**
$$\min_{w,b} \frac{1}{2}\|w\|^2 + C\sum_{i=1}^{n}(\xi_i + \xi_i^*)$$
$$s.t. \quad y_i - w^T\phi(x_i) - b \leq \epsilon + \xi_i$$
$$w^T\phi(x_i) + b - y_i \leq \epsilon + \xi_i^*$$

**本系统参数：**
| 参数 | 搜索范围 | 说明 |
|------|---------|------|
| `kernel` | `rbf`, `linear` | RBF核适合非线性关系，Linear核适合线性关系 |
| `C` | 0.1, 1, 10 | 正则化参数，越大越拟合训练数据但可能过拟合 |
| `gamma` | `scale`, `auto` | RBF核的宽度参数 |

**适用场景：** 数据量中等（< 10K样本）、特征间存在非线性关系时表现优秀。

---

### 1.2 Linear Regression (线性回归)

**原理概述：**
线性回归是最基础的回归方法，假设目标变量与特征之间存在线性关系：$y = w_1x_1 + w_2x_2 + ... + w_nx_n + b$。通过最小二乘法（Ordinary Least Squares）最小化残差平方和来估计参数。

**数学表达：**
$$\hat{w} = \arg\min_w \sum_{i=1}^{n}(y_i - w^Tx_i)^2 = (X^TX)^{-1}X^Ty$$

**优势：**
- 可解释性强：每个系数 $w_i$ 直接表示该特征对睡眠质量的边际影响
- 计算效率高：$O(np^2)$ 时间复杂度
- 作为基线模型，其他算法的提升以此为准

**在睡眠分析中的应用：**
线性回归的系数直接揭示了各因素对睡眠质量的影响方向与大小。例如，`deepSleepRatio` 的正系数表明深睡比例越高，睡眠质量越好。

---

### 1.3 Random Forest (随机森林回归)

**原理概述：**
随机森林是 Bagging（Bootstrap Aggregating）思想的代表算法。它构建多棵决策树，每棵树在随机采样的数据子集和随机选择的特征子集上训练，最终取所有树的预测均值作为结果。双重随机性（样本随机 + 特征随机）有效降低了方差和过拟合风险。

**核心步骤：**
1. 从原始数据集进行 $B$ 次 Bootstrap 采样（有放回抽样）
2. 对每个 Bootstrap 样本训练一棵决策树，每次分裂时从 $p$ 个特征中随机选择 $m \approx \sqrt{p}$ 个候选特征
3. 对 $B$ 棵树的结果取平均

**本系统参数：**
| 参数 | 值 | 说明 |
|------|-----|------|
| `n_estimators` | 100 | 决策树数量 |
| `random_state` | 42 | 随机种子保证可复现 |

**优势：**
- 自动处理非线性关系和特征交互
- 输出特征重要性（Feature Importance），可解释哪些因素对睡眠质量影响最大
- 对缺失值和异常值具有较好的鲁棒性

---

### 1.4 Gradient Boosting (梯度提升回归)

**原理概述：**
梯度提升（Gradient Boosting）是一种 Boosting 方法。与 Bagging 不同，Boosting 通过序贯地训练弱学习器，每个新学习器专注于纠正前一个学习器的残差（误差）。梯度提升使用损失函数的负梯度作为残差的近似，每一步沿着梯度方向添加新的弱学习器。

**数学表达：**
$$F_0(x) = \arg\min_\gamma \sum_{i=1}^{n} L(y_i, \gamma)$$
$$F_m(x) = F_{m-1}(x) + \nu \cdot h_m(x)$$
其中 $h_m(x)$ 是拟合残差 $-\frac{\partial L}{\partial F_{m-1}}$ 的第 $m$ 棵树，$\nu$ 是学习率。

**本系统参数：**
| 参数 | 搜索范围 | 说明 |
|------|---------|------|
| `n_estimators` | 50, 100, 200 | 弱学习器数量 |
| `learning_rate` | 0.05, 0.1, 0.2 | 学习率（收缩参数），小值需更多树 |
| `max_depth` | 3, 5, 7 | 树的深度限制 |

**适用场景：** 对预测精度要求高时，GBRT 通常是最优选择之一，尤其适合表格数据。

---

### 1.5 XGBoost (极端梯度提升)

**原理概述：**
XGBoost（eXtreme Gradient Boosting）是梯度提升的优化实现，引入了以下关键改进：

1. **正则化目标函数：** 在损失函数中加入树的复杂度惩罚项 $\Omega(f) = \gamma T + \frac{1}{2}\lambda\|w\|^2$
2. **二阶泰勒展开：** 利用损失函数的二阶导数信息，收敛更快
3. **列采样（Column Subsampling）：** 类似随机森林的特征随机选择
4. **加权分位数草图（Weighted Quantile Sketch）：** 高效处理大规模数据的近似分裂点查找
5. **稀疏感知（Sparsity-aware）：** 自动处理缺失值

**数学表达：**
$$\mathcal{L}^{(t)} = \sum_{i=1}^{n}[g_i f_t(x_i) + \frac{1}{2}h_i f_t^2(x_i)] + \Omega(f_t)$$

其中 $g_i = \partial_{\hat{y}^{(t-1)}} l(y_i, \hat{y}^{(t-1)})$ 是一阶梯度，$h_i$ 是二阶梯度。

**本系统参数：**
| 参数 | 值 | 说明 |
|------|-----|------|
| `n_estimators` | 100 | 树的数量 |
| `learning_rate` | 0.1 | 学习率 |
| `max_depth` | 6 | 树的最大深度 |

**注意：** XGBoost 为可选依赖，需通过 `pip install xgboost` 安装。若未安装，系统会自动跳过此模型。

---

### 1.6 Decision Tree (决策树回归)

**原理概述：**
决策树通过递归地划分特征空间来构建预测模型。在每个内部节点，选择最优特征和分裂点使得划分后的子节点内样本方差最小（回归树使用 MSE 作为分裂准则）。叶节点的预测值为该节点内所有样本的均值。

**分裂准则（CART 回归树）：**
$$\Delta = \frac{m_{left}}{m} \cdot MSE_{left} + \frac{m_{right}}{m} \cdot MSE_{right}$$

**本系统参数：**
| 参数 | 搜索范围 | 说明 |
|------|---------|------|
| `max_depth` | 3, 5, 7, 10, None | 最大深度，None 表示不限制 |
| `min_samples_split` | 2, 5, 10 | 内部节点再划分所需最小样本数 |

**定位：** 作为树模型的基准（baseline），用于对比集成方法（RF、GBRT）的提升效果。

---

### 1.7 Ridge Regression (岭回归)

**原理概述：**
岭回归在线性回归的损失函数中加入 L2 正则化项，通过惩罚大系数来防止过拟合。L2 正则化会使所有系数向零收缩但不会完全归零。

**数学表达：**
$$\hat{w}_{ridge} = \arg\min_w \left\{ \sum_{i=1}^{n}(y_i - w^Tx_i)^2 + \alpha \sum_{j=1}^{p} w_j^2 \right\}$$

**解析解：**
$$\hat{w}_{ridge} = (X^TX + \alpha I)^{-1}X^Ty$$

**本系统参数：**
| 参数 | 搜索范围 | 说明 |
|------|---------|------|
| `alpha` | 0.01, 0.1, 1.0, 10.0, 100.0 | 正则化强度，越大惩罚越强 |

**适用场景：** 特征之间存在多重共线性（高度相关）时，岭回归比普通线性回归更稳定。

---

### 1.8 Lasso Regression (套索回归)

**原理概述：**
Lasso（Least Absolute Shrinkage and Selection Operator）使用 L1 正则化。与 Ridge 不同，L1 正则化有特征选择的效果——不重要特征的系数会被精确压缩为零。

**数学表达：**
$$\hat{w}_{lasso} = \arg\min_w \left\{ \frac{1}{2n}\sum_{i=1}^{n}(y_i - w^Tx_i)^2 + \alpha \sum_{j=1}^{p} |w_j| \right\}$$

**L1 vs L2 对比：**
- L2 (Ridge)：系数收缩但不为零，所有特征都保留
- L1 (Lasso)：系数可收缩到零，自动特征选择

**本系统参数：**
| 参数 | 搜索范围 | 说明 |
|------|---------|------|
| `alpha` | 0.001, 0.01, 0.1, 1.0 | 正则化强度 |

**在睡眠分析中的应用：**
Lasso 可以自动识别出对睡眠质量真正重要的特征，过滤掉噪声特征。例如，如果 `dayCalories` 系数被压缩为零，说明卡路里消耗对睡眠质量无显著独立影响。

---

### 1.9 ElasticNet (弹性网络)

**原理概述：**
弹性网络结合了 L1 和 L2 正则化的优点，通过混合参数 $\rho$ (l1_ratio) 控制两者的权重。它既能像 Lasso 一样进行特征选择，又能像 Ridge 一样处理共线性。

**数学表达：**
$$\hat{w}_{elastic} = \arg\min_w \left\{ \frac{1}{2n}\sum_{i=1}^{n}(y_i - w^Tx_i)^2 + \alpha\rho\sum_{j=1}^{p}|w_j| + \frac{\alpha(1-\rho)}{2}\sum_{j=1}^{p}w_j^2 \right\}$$

**本系统参数：**
| 参数 | 搜索范围 | 说明 |
|------|---------|------|
| `alpha` | 0.01, 0.1, 1.0 | 总正则化强度 |
| `l1_ratio` | 0.1, 0.5, 0.7, 0.9 | L1占比，0=纯Ridge，1=纯Lasso |

---

### 1.10 KNN Regression (K近邻回归)

**原理概述：**
KNN 是一种非参数、基于实例的学习方法。对于一个新的输入样本，找到训练集中距离最近的 K 个样本，用它们的标签均值（或距离加权均值）作为预测值。KNN 不进行显式训练，所有计算都在预测时进行（惰性学习）。

**距离度量（欧氏距离）：**
$$d(x, x') = \sqrt{\sum_{j=1}^{p}(x_j - x'_j)^2}$$

**本系统参数：**
| 参数 | 搜索范围 | 说明 |
|------|---------|------|
| `n_neighbors` | 3, 5, 7, 9, 11 | 邻居数量 |
| `weights` | `uniform`, `distance` | uniform=等权平均，distance=距离倒数加权 |

**局限性：** 对特征尺度敏感（需要标准化），高维数据下效果下降（维度灾难）。

---

### 1.11 Bayesian Ridge (贝叶斯岭回归)

**原理概述：**
贝叶斯岭回归将线性回归纳入贝叶斯框架。它假设权重 $w$ 服从高斯先验分布，通过贝叶斯推断自动从数据中估计正则化参数 $\alpha$ 和噪声精度 $\lambda$，无需手动调参。

**概率模型：**
$$p(y|X, w, \alpha) = \mathcal{N}(y|Xw, \alpha^{-1}I)$$
$$p(w|\lambda) = \mathcal{N}(w|0, \lambda^{-1}I)$$

**优势：**
- 自动调节正则化强度，无需交叉验证
- 提供预测的不确定性估计
- 对小样本数据适应良好

---

### 1.12 AdaBoost Regression (自适应增强回归)

**原理概述：**
AdaBoost（Adaptive Boosting）是 Boosting 家族的开创性算法。与 Gradient Boosting 不同，AdaBoost 通过调整样本权重来实现序贯学习：每个新学习器更关注前一个学习器预测错误的样本（权重增加），预测正确的样本权重降低。最终预测是所有弱学习器的加权中位数。

**权重更新公式：**
$$w_i^{(m+1)} = w_i^{(m)} \cdot \exp(\alpha_m \cdot |y_i - f_m(x_i)|)$$

**本系统参数：**
| 参数 | 搜索范围 | 说明 |
|------|---------|------|
| `n_estimators` | 50, 100, 200 | 弱学习器数量 |
| `learning_rate` | 0.5, 1.0, 1.5 | 学习率 |

---

## <a id="分类算法"></a>2. 分类算法（预测睡眠阶段）

### 2.1 Random Forest Classifier (随机森林分类)

用于根据生理信号和环境参数预测睡眠阶段（清醒/浅睡/深睡/REM）。原理与回归版本类似，但使用多数投票而非均值作为最终预测。

### 2.2 Logistic Regression (逻辑回归)

多分类逻辑回归（softmax regression）作为分类基线模型，计算每个睡眠阶段的后验概率：
$$P(y=k|x) = \frac{\exp(w_k^Tx)}{\sum_{j=1}^{K}\exp(w_j^Tx)}$$

### 2.3 KNN Classifier (K近邻分类)

基于最近邻居的多数投票预测睡眠阶段。

---

## <a id="数据预处理"></a>3. 数据预处理方法

### 3.1 线性插值 (Linear Interpolation)
填充时间序列中的缺失值，假设数据在时间上是连续的。
$$y_t = y_{t-1} + \frac{y_{t+k} - y_{t-1}}{k}$$

### 3.2 滑动窗口平滑 (Sliding Window Smoothing)
使用移动平均降低心率等信号的噪声：
$$\hat{x}_t = \frac{1}{2m+1}\sum_{i=-m}^{m} x_{t+i}$$

### 3.3 IQR 异常值剔除 (Interquartile Range)
使用四分位距识别并标记异常值：
$$\text{异常值} = x < Q_1 - 1.5 \times IQR \quad \text{或} \quad x > Q_3 + 1.5 \times IQR$$

### 3.4 Z-score 异常值检测
标准分数法：$|z| > 3$ 视为异常值。
$$z = \frac{x - \mu}{\sigma}$$

### 3.5 特征工程
- **睡眠效率** = 总睡眠时间 / (总睡眠时间 + 清醒时间)
- **深睡比例** = 深睡时间 / 总睡眠时间
- **REM比例** = REM时间 / 总睡眠时间
- **心率趋势** = 心率的一阶差分
- **周期性特征** = 星期几、是否周末

---

## <a id="评估指标"></a>4. 模型评估指标

| 指标 | 公式 | 说明 | 理想值 |
|------|------|------|--------|
| **R² (决定系数)** | $R^2 = 1 - \frac{SS_{res}}{SS_{tot}}$ | 模型解释的方差比例 | 接近 1 |
| **MAE (平均绝对误差)** | $MAE = \frac{1}{n}\sum\|y_i - \hat{y}_i\|$ | 预测误差的平均幅度 | 接近 0 |
| **RMSE (均方根误差)** | $RMSE = \sqrt{\frac{1}{n}\sum(y_i - \hat{y}_i)^2}$ | 对大误差更敏感 | 接近 0 |

系统使用 80/20 训练/测试集分割，通过 GridSearchCV 进行交叉验证调参。

---

## <a id="模型对比"></a>5. 模型对比与选择

### 对比维度

所有回归模型在相同的训练/测试集上评估，对比维度包括：

1. **预测精度：** R²、MAE、RMSE
2. **可解释性：** 特征重要性 / 系数
3. **计算效率：** 训练时间
4. **鲁棒性：** 对异常值和缺失值的容忍度

### 选择策略

系统采用 **R² 最大化策略** 自动选择最佳模型进行预测。用户可在前端界面上查看所有模型的对比指标（R²、MAE、RMSE），进行横向对比。

### 算法适用性总结

| 场景 | 推荐算法 |
|------|---------|
| 需要高预测精度 | Gradient Boosting / XGBoost |
| 需要可解释性 | Linear Regression / Ridge / Lasso |
| 数据量较小 (< 100) | Bayesian Ridge / Ridge |
| 数据存在非线性关系 | Random Forest / SVR (RBF) |
| 需要特征选择 | Lasso / ElasticNet |
| 对异常值敏感 | Random Forest / Gradient Boosting |

---


